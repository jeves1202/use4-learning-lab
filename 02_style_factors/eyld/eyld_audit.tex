\documentclass[11pt,a4paper]{article}

\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath, amssymb}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{graphicx}
\usepackage{caption}
\usepackage{subcaption}
\usepackage[table]{xcolor}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{url}
\usepackage[hidelinks,bookmarks=true,bookmarksopen=true]{hyperref}
\usepackage{siunitx}
\usepackage{parskip}

\sisetup{group-separator={,}, detect-weight=true, detect-family=true}
\renewcommand{\arraystretch}{1.15}

\definecolor{pass}{RGB}{0,140,0}
\definecolor{warn}{RGB}{200,120,0}
\definecolor{fail}{RGB}{180,0,0}
\definecolor{note}{RGB}{60,60,180}
\definecolor{accent}{HTML}{01696F}
\definecolor{ink}{HTML}{28251D}
\definecolor{muted}{HTML}{7A7974}
\definecolor{rule}{HTML}{D4D1CA}

\newcommand{\PASS}[1]{\textcolor{pass}{\textbf{#1}}}
\newcommand{\WARN}[1]{\textcolor{warn}{\textbf{#1}}}
\newcommand{\FAIL}[1]{\textcolor{fail}{\textbf{#1}}}
\newcommand{\NOTE}[1]{\textcolor{note}{\textit{#1}}}

\titleformat{\section}
  {\normalfont\large\bfseries\color{accent}}{}{0pt}{}
  [\vspace{-2pt}\textcolor{rule}{\rule{\linewidth}{0.4pt}}]

\titleformat{\subsection}
  {\normalfont\normalsize\bfseries\color{ink}}{}{0pt}{}

\titlespacing*{\section}{0pt}{14pt}{6pt}
\titlespacing*{\subsection}{0pt}{10pt}{2pt}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\fancyfoot[L]{\footnotesize\color{muted}use4-learning-lab --- reference run 2026-06-11}
\fancyfoot[C]{\footnotesize\color{muted}\leftmark}
\fancyfoot[R]{\footnotesize\color{muted}\thepage}

\title{\textbf{Earnings Yield (EYLD) --- Factor Construction Audit}\\[2pt]
\large USE4-Faithful Implementation --- \texttt{eyld\_build.ipynb}\\[4pt]
\normalsize USE4 Learning Lab --- Reference Audit}
\author{Audit Date: 2026-06-11}
\date{}

\begin{document}
\maketitle
\tableofcontents
\newpage

%──────────────────────────────────────────────────────────────────────────────
\section{Purpose and Scope}

Audits the Earnings Yield factor:
$\mathrm{EYLD} = 0.6\,\mathrm{CETOP} + 0.4\,\mathrm{ETOP}$ (USE4 Empirical Notes,
Appendix~A), with $\mathrm{ETOP} = \text{TTM netinc}/\text{mcap}$ and
$\mathrm{CETOP} = (\text{TTM netinc} + \text{TTM depamor})/\text{mcap}$ as the
cash-earnings proxy.

\smallskip
\textbf{Scope.} This audit covers every pipeline stage of
\texttt{eyld\_build.ipynb} from the lab's reference run of 2026-06-11: all printed diagnostics, the
validation battery, and the output parquet schema. All numerical values are
verbatim from the executed notebook. The notebook generates no figures.

\NOTE{Reference numbers: every value in this document comes verbatim from the lab's reference run of 2026-06-11 (Sharadar data through 2026-06-10). Your rebuild will differ in row counts and tail statistics with data vintage; distribution shapes, stability coefficients, and check outcomes should match. Treat persistent sign or order-of-magnitude differences as bugs in your build until proven otherwise.}


%──────────────────────────────────────────────────────────────────────────────
\section{Output Schema}

The deliverable is \path{data/out/eyld_use4.parquet}, written with
\texttt{compression="zstd"}, \newline\texttt{statistics=True}.

\begin{table}[h!]
\centering
\caption{Output schema}
\begin{tabular}{llp{7.4cm}}
\toprule
\textbf{Column} & \textbf{Dtype} & \textbf{Description} \\
\midrule
\texttt{permaticker} & \texttt{Int64} & Sharadar permanent ticker ID \\
\texttt{signal\_date} & \texttt{Date} & End-of-month rebalance date \\
\texttt{in\_estu} & \texttt{Bool} & ESTU membership on \texttt{signal\_date} \\
\texttt{mcap} & \texttt{Float64} & Market cap on \texttt{signal\_date} (cap-weighting) \\
\texttt{eyld} & \texttt{Float64} & Raw descriptor: $0.6\,\mathrm{CETOP} + 0.4\,\mathrm{ETOP}$ \\
\texttt{eyld\_score} & \texttt{Float64} & Final standardised exposure (the deliverable) \\
\texttt{n\_obs} & \texttt{UInt32} & 1 if a valid TTM observation within the lookback window \\
\bottomrule
\end{tabular}
\end{table}

%──────────────────────────────────────────────────────────────────────────────
\section{Shared Infrastructure}

All factor builds consume the shared estimation universe
\path{data/out/estu_monthly.parquet} (audited separately in
\texttt{estu\_audit.tex}); the time-series builds additionally consume the
shared daily-returns artifact (audited in \texttt{daily\_panel\_audit.tex}).
On this run the factor-level view of the shared inputs was:

\begin{itemize}[leftmargin=*,topsep=2pt,itemsep=2pt]
  \item Signal calendar: \textbf{330 dates} (1999-01-29 $\to$ 2026-06-10)
  \item ESTU size: mean \textbf{2{,}494}, min \textbf{1{,}791}, max \textbf{3{,}045}
\end{itemize}

%──────────────────────────────────────────────────────────────────────────────
\section{Known Deviations from PDF Spec}

This section mirrors the complete NOT-IN-PDF master list from
\texttt{eyld\_spec.ipynb} (\S13.) --- every judgment call the USE4 PDFs do not
specify, with the chosen solution and its revisit trigger. Rejected alternatives
are documented in the guide; the four-part validity arguments (USE4 requirement
$\to$ deviation $\to$ solution $\to$ defence) live in the pipeline-level
register, \path{docs/deviations_register.tex}.

\begin{longtable}{@{}p{0.5cm}p{3.6cm}p{6.0cm}p{3.8cm}@{}}
\caption{NOT-IN-PDF decisions (mirrors the guide master list)}\\
\toprule
\textbf{\#} & \textbf{Decision} & \textbf{Chosen solution} & \textbf{Revisit when} \\
\midrule
\endhead
1 & EPFWD omitted --- no forward estimates in database \WARN{Material} & Renormalize to 0.6$\cdot$CETOP + 0.4$\cdot$ETOP & If forward estimate data becomes available \\[3pt]
2 & Data source for fundamentals & Sharadar SF1 ARQ; TTM constructed as 4-quarter rolling sums & Never (Sharadar is the lab's data stack) \\[3pt]
3 & PIT alignment & Use \texttt{datekey} (filing date) as PIT constraint & If Sharadar datekey quality is suspected \\[3pt]
4 & Cash earnings definition & netinc + depamor & If different definition matches USE4 intent better \\[3pt]
5 & mcap denominator source & estu\_monthly mcap on signal\_date & Never --- signal\_date mcap is always correct for E/P ratios \\[3pt]
6 & Negative earnings treatment & Allow; 3$\sigma$ trim handles extremes & If factor is used in a value-only context \\[3pt]
7 & Staleness cutoff & 548 calendar days ($\sim$18 months) & If stale fundamentals are suspected to add noise \\[3pt]
8 & Financial sector treatment & No special treatment; include all sectors & If financial sector distorts the factor \\[3pt]
9 & MIN\_OBS threshold & 1 (binary: has valid TTM or not) & If n\_obs proves uninformative \\[3pt]
10 & Calendar start & 1999-01-01 & If early data quality is poor \\[3pt]
\bottomrule
\end{longtable}

%──────────────────────────────────────────────────────────────────────────────
\section{Stage-by-Stage Audit}

%──────────────────────────────────
\subsection{Stage 1 --- SF1 $\to$ TTM earnings}

\begin{description}[style=nextline]
  \item[PDF SPEC] Trailing 12-month earnings and cash earnings.
  \item[Implementation] ARQ netinc/depamor rolled into 4-quarter sums (requires 4 complete quarters).
  \item[Observed]\phantom{x}
    \begin{itemize}[nosep, topsep=-\parskip]
      \item TTM rows: \textbf{567{,}298}; \textbf{15{,}250} tickers (1999-01-04 $\to$ 2026-06-10)
      \item netinc nulls \textbf{0}; depamor nulls \textbf{0}
      \item \NOTE{Two polars DeprecationWarnings (} \texttt{min\_periods} \NOTE{$\to$} \texttt{min\_samples}\NOTE{) --- cosmetic, behaviour unchanged}
    \end{itemize}
  \item[Status] \PASS{MATCHES SPEC}
\end{description}

%──────────────────────────────────
\subsection{Stage 2 --- Load shared ESTU}

\begin{description}[style=nextline]
  \item[PDF SPEC] USE4 ESTU = MSCI USA IMI; the project proxy is the shared \path{estu_monthly.parquet} deliverable.
  \item[Implementation] Reads \path{data/out/estu_monthly.parquet}, filtered to \texttt{signal\_date} $\geq$ 1999-01-01.
  \item[Observed]\phantom{x}
    \begin{itemize}[nosep, topsep=-\parskip]
      \item Signal calendar: \textbf{330 dates} (1999-01-29 $\to$ 2026-06-10)
      \item ESTU universe rows: \textbf{1{,}773{,}820}
      \item ESTU size: mean \textbf{2{,}494}, min \textbf{1{,}791}, max \textbf{3{,}045}
    \end{itemize}
  \item[Status] \PASS{MATCHES SPEC}
\end{description}

%──────────────────────────────────
\subsection{Stage 4 --- EYLD estimator (PIT join)}

\begin{description}[style=nextline]
  \item[PDF SPEC] Composite of earnings-to-price descriptors.
  \item[Implementation] \texttt{join\_asof} backward on \texttt{datekey}; staleness $\leq548$ days; \texttt{eyld} $= 0.6\,(\text{netinc}+\text{depamor})/\text{mcap} + 0.4\,\text{netinc}/\text{mcap}$.
  \item[Observed]\phantom{x}
    \begin{itemize}[nosep, topsep=-\parskip]
      \item Rows with SF1 hit: \textbf{1{,}548{,}088}; panel \textbf{1{,}537{,}393} rows, \textbf{14{,}933} tickers; duplicates \textbf{0}
      \item Spot 2026-05-29: \textbf{4{,}210} tickers, median eyld \textbf{0.0332}
    \end{itemize}
  \item[Status] \PASS{MATCHES SPEC}
\end{description}

%──────────────────────────────────
\subsection{Stage 5 --- Outlier trim (3$\sigma$)}

\begin{description}[style=nextline]
  \item[PDF SPEC] Methodology \S2.2 trim.
  \item[Implementation] CW mean $\pm\,3\cdot$EW std (ESTU bounds, applied to all).
  \item[Observed]\phantom{x}
    \begin{itemize}[nosep, topsep=-\parskip]
      \item Clipped (ESTU): \textbf{10{,}216} / 791{,}992 = \textbf{1.29\%} (target 0.5--2\%)
      \item Bounds: lo mean $-0.5693$; hi mean $0.6875$
      \item Post-trim at 2026-05-29 (ESTU, $n=2{,}708$): mean 0.0266, std 0.1303, skew $-1.388$, kurt 7.885
    \end{itemize}
  \item[Status] \PASS{MATCHES SPEC}
\end{description}

%──────────────────────────────────
\subsection{Stage 6 --- Standardisation}

\begin{description}[style=nextline]
  \item[PDF SPEC] CW mean 0 (ESTU), EW std 1 (ESTU), applied to all stocks.
  \item[Implementation] Standard shared machinery.
  \item[Observed]\phantom{x}
    \begin{itemize}[nosep, topsep=-\parskip]
      \item CW mean: \textbf{$-$0.000000}; EW std: \textbf{1.000000}
      \item Fraction in $[\pm3]$: all \textbf{87.5\%} / ESTU \textbf{97.2\%}
    \end{itemize}
  \item[Status] \PASS{MATCHES SPEC}
\end{description}

%──────────────────────────────────
\subsection{Stage 7 --- Save}

\begin{description}[style=nextline]
  \item[PDF SPEC] Parquet deliverable with asserted schema.
  \item[Implementation] zstd; dtype + read-back assertions.
  \item[Observed]\phantom{x}
    \begin{itemize}[nosep, topsep=-\parskip]
      \item Rows \textbf{1{,}537{,}393}; dates \textbf{330}; tickers \textbf{14{,}933}; \textbf{26.5 MB}
      \item Schema check \PASS{PASSED}; read-back \PASS{PASSED}
    \end{itemize}
  \item[Status] \PASS{MATCHES SPEC}
\end{description}

%──────────────────────────────────────────────────────────────────────────────
\section{Validation Checks}

All checks passed on the run dated 2026-06-11. Coverage (check 3) is evaluated
on completed months only --- the final in-progress signal date is excluded
(shared convention across all factor builds).

\begin{table}[h!]
\centering
\caption{Validation results --- run date 2026-06-11}
\small
\begin{tabular}{clll}
\toprule
\textbf{Check} & \textbf{Description} & \textbf{Observed} & \textbf{Target / Status} \\
\midrule
1a & Max $|\mu_{\text{CW}}(\texttt{eyld\_score})|$ & $1.90\times10^{-16}$ & $<10^{-6}$ \quad \PASS{PASS} \\
1b & Mean EW std & $1.0000$ & $1.0\pm0.02$ \quad \PASS{PASS} \\
2a & Median raw eyld (ESTU) & $0.05891$ & $>0.0$ \quad \PASS{PASS} \\
2b & Dates with positive ESTU median & $100.0\%$ & $>70\%$ \quad \PASS{PASS} \\
3 & Min coverage post-2005 (completed months) & $4{,}151$ & $\geq4{,}000$ \quad \PASS{PASS} \\
4 & MoM Spearman $\rho$ mean & $0.9630$ & $>0.85$ \quad \PASS{PASS} \\
5 & Q1$\to$Q5 mean eyld monotone & spread $0.26033$ & monotone \quad \PASS{PASS} \\
6 & Disk vs memory max diff & $0.00\times10^{0}$ & $<10^{-10}$ \quad \PASS{PASS} \\
\bottomrule
\end{tabular}
\end{table}

\begin{itemize}[leftmargin=*,topsep=2pt,itemsep=2pt]
  \item \textbf{Check 5 quintile means:} Q1 $=-0.09620$, Q2 $=0.03215$, Q3 $=0.05821$, Q4 $=0.08431$, Q5 $=0.16414$.
\end{itemize}

%──────────────────────────────────────────────────────────────────────────────
\section{Spot Check at 2026-05-29}

Aggregate diagnostics at the penultimate signal date:

\begin{itemize}[leftmargin=*,topsep=2pt,itemsep=2pt]
  \item Coverage: \textbf{4{,}210} tickers; median eyld \textbf{0.0332}; ESTU \textbf{2{,}708} stocks
  \item Post-trim ESTU: mean \textbf{0.0266}, std \textbf{0.1303}, skew \textbf{$-$1.388}, excess kurtosis \textbf{7.885}
\end{itemize}

\NOTE{No per-ticker factor values were printed by the build notebook
; the spot check reports aggregate statistics only.}

\end{document}
